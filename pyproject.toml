[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--import-mode=importlib"
python_files = ["test_*.py"]

[project]
name = "hermes-intercom-tests"
version = "0.0.0"
requires-python = ">=3.11"
